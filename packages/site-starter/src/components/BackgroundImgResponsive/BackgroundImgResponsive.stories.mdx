import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
// import { action } from '@storybook/addon-actions'
import { BackgroundImgResponsive } from './index'

<Meta title="BackgroundImgResponsive" component={BackgroundImgResponsive} />

# BackgroundImg

With `MDX` we can define a story for `BackgroundImg` right in the middle of our
markdown documentation.

<Preview>
  <Story name="Responsive Background Image with Gatsby">
    <BackgroundImgResponsive />
  </Story>
</Preview>
